# Default config for the demo — intentionally left empty so every smell
# on every added line is reported. Copy one of the files in configs/ over
# this file to try each filter scenario.
#
# Examples:
#   cp configs/01-filter-by-smell-types.yml .rubycritic-pronto.yml
#   cp configs/06-complexity-max.yml        .rubycritic-pronto.yml
